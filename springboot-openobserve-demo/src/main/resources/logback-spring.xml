<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!--
        Console appender: prints logs locally with traceId and spanId embedded.
        This is what you see in the terminal when the app is running.
        The traceId here is the same one you'll find in OpenObserve — enabling
        direct correlation between your terminal output and the dashboard.
    -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - traceId=%X{traceId} spanId=%X{spanId} - %msg%n</pattern>
        </encoder>
    </appender>

    <!--
        OpenTelemetry appender: forwards every log record to OpenObserve via OTLP.
        - captureExperimentalAttributes: includes extra OTel attributes on each log
        - captureMdcAttributes: captures all MDC fields (including traceId, spanId)
        The SDK is injected at startup via OpenTelemetryConfig#installLogbackAppender().
    -->
    <appender name="OpenTelemetry"
              class="io.opentelemetry.instrumentation.logback.appender.v1_0.OpenTelemetryAppender">
        <captureExperimentalAttributes>true</captureExperimentalAttributes>
        <captureKeyValuePairAttributes>true</captureKeyValuePairAttributes>
        <captureLoggerContext>true</captureLoggerContext>
        <captureMdcAttributes>*</captureMdcAttributes>
    </appender>

    <!-- Root logger: all INFO+ logs go to both console and OpenObserve -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="OpenTelemetry"/>
    </root>

    <!-- Application code logs at DEBUG so you can see the full detail in the demo -->
    <logger name="com.example.demo" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="OpenTelemetry"/>
    </logger>

</configuration>
